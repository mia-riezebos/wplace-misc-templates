{
  "name": "wplace-misc-templates",
  "version": "0.6.13",
  "private": true,
  "packageManager": "pnpm@11.13.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "tsc --noEmit",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "verify": "pnpm check && pnpm test && pnpm build"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "esbuild": "^0.27.0",
    "typescript": "^5.9.2"
  }
}
